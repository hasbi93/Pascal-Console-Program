program menghitungjaraktempuh;

var
v,s,t:longint;

begin
writeln('masukkan nilai v(kec. rata rata) dan t(waktu tempuh)');
readln(v,t);
s:=v*t;
writeln('jarak yang sudah ditempuh adalah, ',s,' km.');
end.